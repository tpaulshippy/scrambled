<div class="text-center">
  <h1 class="text-4xl font-bold text-gray-900 mb-8">Word Scramble</h1>
  <p class="text-lg text-gray-600 mb-8">Multiplayer word game for 4-12+ players</p>
  
  <div class="max-w-md mx-auto space-y-4">
    <%= link_to "Create New Game", new_game_path, class: "block w-full bg-blue-600 text-white py-3 px-6 rounded-lg text-lg font-semibold hover:bg-blue-700 transition" %>
    
    <div class="text-gray-500">or</div>
    
    <%= form_with url: games_path, method: :get, local: true, class: "space-y-4" do |f| %>
      <%= f.text_field :code, placeholder: "Enter 4-digit game code", maxlength: 4, 
          class: "w-full px-4 py-3 border border-gray-300 rounded-lg text-center text-2xl font-mono tracking-widest focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
      <%= f.submit "Join Game", class: "w-full bg-green-600 text-white py-3 px-6 rounded-lg text-lg font-semibold hover:bg-green-700 transition" %>
    <% end %>
  </div>
  
  <% if @games.present? %>
    <div class="mt-12">
      <h2 class="text-2xl font-bold text-gray-900 mb-6">Active Games</h2>
      <div class="grid gap-4 max-w-2xl mx-auto">
        <% @games.each do |game| %>
          <div class="bg-white border border-gray-200 rounded-lg p-6 shadow-sm">
            <div class="flex justify-between items-center">
              <div>
                <div class="text-2xl font-mono font-bold text-blue-600"><%= game.code %></div>
                <div class="text-sm text-gray-500">
                  <%= pluralize(game.players.count, 'player') %> • 
                  <%= game.state.titleize %>
                </div>
              </div>
              <%= link_to "Join", join_game_path(game), 
                  class: "bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
</div>
